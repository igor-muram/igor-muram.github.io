@inherits ReactiveComponentBase<Telma.ProjectSystem.MHSplineDefinedProperty>

<Label Text=@("Curve name: " + ViewModel.Curve.CurveName) IsVisible=@IsVisible />

@code
{
    [Parameter] public bool IsVisible { get; set; } = true;
}
